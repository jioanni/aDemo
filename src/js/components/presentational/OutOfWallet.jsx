import React from "react";
import Input from "./Input.jsx";
const OutOfWallet = ({ label, text, type, id, value, handleChange }) => (
    <Input>
    {/*This is going to wind up being a looped generation of each of the questions and it's answers as Input components, but as Radio types*/}
    </Input>
);
export default OutOfWallet;